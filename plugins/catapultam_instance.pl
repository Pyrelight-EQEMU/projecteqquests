sub ProcessInstanceDialog {
    quest::debug("ProcessInstanceDialog Start");

    my $text   = plugin::val('text');
    my $client = plugin::val('client');
    my $npc    = plugin::val('npc');

    quest::debug("debug: $text, $client, $npc");
}